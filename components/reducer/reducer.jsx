import React from 'react'

const Reducer = (state,action) => {

switch (action.type) {
    case "SET_ITEM_VIEW":
        console.log("react")
        return( 
            ...state,
            items:
        )

    default:
       return ( 
         ...state)
}

  
}

export default Reducer;